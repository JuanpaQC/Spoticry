// Frontend-only preview mode. While this flag stays enabled, the UI should
// avoid assuming persistence or backend-backed data.
const frontendPreview = {
  enabled: true,
  persistsChanges: false,
}

export { frontendPreview }
